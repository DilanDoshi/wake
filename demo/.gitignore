# The recording workspace: generated by setup.sh, holds absolute paths, a
# scratch HOME, a built binary and the raw takes. Nothing here is source.
.work/

# Masters. Rebuilt by build.sh from the tapes; too large to version.
wake-demo.mp4
wake-demo.gif

# Generated by page.py — 1.8MB of inlined video.
wake-demo-page.html
